name: pooch
channels:
    - conda-forge
    - defaults
dependencies:
    - python==3.11
    - pip
    # Run
    - requests
    - packaging
    - platformdirs
    # Optional dependencies
    - tqdm>=4.41.0,<5.0.0
    - paramiko>=2.7.0
    - xxhash>=1.4.3
    # Build
    - build
    # Test
    - pytest
    - pytest-cov
    - pytest-localftpserver
    - pytest-httpserver
    - coverage
    # Documentation
    - sphinx==7.2.*
    - sphinx-book-theme==1.1.*
    - sphinx-design==0.5.*
    # Style
    - pathspec
    - black>=20.8b1
    - flake8
    - pylint>=2.4
    - pip:
      - burocrata
