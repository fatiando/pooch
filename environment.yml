name: pooch
channels:
    - conda-forge
dependencies:
    - python==3.11
    - pip
    # Run
    - requests
    - packaging
    - platformdirs
    # Build
    - build
    # Test
    - pytest
    - pytest-cov
    - pytest-localftpserver
    - coverage
    # Documentation
    - sphinx ==4.4.*
    - sphinx-book-theme ==0.2.*
    - sphinx-panels ==0.6.*
    # Style
    - pathspec
    - black >=20.8b1
    - flake8
    - pylint >=2.4
