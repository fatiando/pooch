name: pooch
channels:
    - conda-forge
    - defaults
dependencies:
    - python==3.11
    - pip
    # Run
    - requests
    - packaging
    - platformdirs
    # Build
    - build
    # Test
    - pytest
    - pytest-cov
    - pytest-localftpserver
    - pytest-httpserver
    - coverage
    # Documentation
    - sphinx==7.2.*
    - sphinx-book-theme==1.1.*
    - sphinx-design==0.5.*
    # Style
    - pathspec
    - black>=20.8b1
    - flake8
    - pylint>=2.4
    - pip:
      - burocrata
