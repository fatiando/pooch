name: pooch
channels:
    - conda-forge
    - defaults
dependencies:
    - python==3.11
    - pip
    # Run
    - requests
    - packaging
    - platformdirs
    # Build
    - build
    # Test
    - pytest
    - pytest-cov
    - pytest-localftpserver
    - pytest-httpserver
    - coverage
    # Documentation
    - sphinx==4.4.*
    - sphinx-book-theme==0.2.*
    - sphinx-panels==0.6.*
    # Style
    - pathspec
    - black>=20.8b1
    - flake8
    - pip:
      - pylint==2.4.*
